import { Component } from "solid-js";

const Analyzer: Component = () => {
    return (<>hi unfortunately linguistics is very hard so maybe I'll make this a stats page instead unless YOU (points at you) have other ideas</>);
}

export default Analyzer;